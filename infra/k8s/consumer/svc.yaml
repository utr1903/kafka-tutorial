apiVersion: v1
kind: Service
metadata:
  name: consumer
  namespace: cons
  labels:
    app: consumer
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: consumer
