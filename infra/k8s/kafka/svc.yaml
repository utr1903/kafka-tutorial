apiVersion: v1
kind: Service
metadata:
  name: kafka-0
  namespace: kafka
  labels:
    app: kafka-0
spec:
  clusterIP: None
  ports:
    - port: 9092
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: kafka-0
